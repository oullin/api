apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: ${GF_DATASOURCE_PROMETHEUS_URL}
    isDefault: true
    editable: true
    allowUiUpdates: true
    jsonData:
      timeInterval: 15s
      queryTimeout: 60s
